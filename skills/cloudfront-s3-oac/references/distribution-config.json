{
  "_comment": "Ready-to-edit config for: aws cloudfront create-distribution --distribution-config file://this.json. Replace every <PLACEHOLDER>. The OAC must already exist (create-origin-access-control) and its Id goes in OriginAccessControlId. After creation, attach the bucket policy that trusts this distribution's ARN (see SKILL.md). CallerReference must be unique per create call.",
  "CallerReference": "<UNIQUE_STRING_OR_TIMESTAMP>",
  "Comment": "CloudFront for <BUCKET_NAME> via OAC",
  "Enabled": true,
  "DefaultRootObject": "index.html",
  "Origins": {
    "Quantity": 1,
    "Items": [
      {
        "Id": "s3-<BUCKET_NAME>",
        "DomainName": "<BUCKET_NAME>.s3.<REGION>.amazonaws.com",
        "OriginAccessControlId": "<OAC_ID>",
        "S3OriginConfig": {
          "OriginAccessIdentity": ""
        },
        "ConnectionAttempts": 3,
        "ConnectionTimeout": 10,
        "OriginShield": {
          "Enabled": false
        }
      }
    ]
  },
  "DefaultCacheBehavior": {
    "TargetOriginId": "s3-<BUCKET_NAME>",
    "ViewerProtocolPolicy": "redirect-to-https",
    "AllowedMethods": {
      "Quantity": 2,
      "Items": ["GET", "HEAD"],
      "CachedMethods": {
        "Quantity": 2,
        "Items": ["GET", "HEAD"]
      }
    },
    "Compress": true,
    "CachePolicyId": "658327ea-f89d-4fab-a63d-7e88639e58f6",
    "_CachePolicyId_comment": "658327ea-f89d-4fab-a63d-7e88639e58f6 is the AWS managed CachingOptimized policy (same ID for every account)"
  },
  "PriceClass": "PriceClass_All",
  "ViewerCertificate": {
    "_comment": "For a custom domain, set ACMCertificateArn (cert must be in us-east-1), SSLSupportMethod=sni-only, MinimumProtocolVersion=TLSv1.2_2021, and add the domain to Aliases below. For the default *.cloudfront.net domain only, keep CloudFrontDefaultCertificate=true and leave Aliases empty.",
    "CloudFrontDefaultCertificate": true
  },
  "Aliases": {
    "Quantity": 0,
    "Items": []
  }
}
